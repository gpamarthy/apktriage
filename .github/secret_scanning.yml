# Test fixtures intentionally contain fabricated, secret-shaped strings (a fake
# Google API key, an AWS example key, a sample C2 URL) so the detector tests have
# something to find. They are not real credentials. Exclude test paths from
# secret-scanning alerts.
paths-ignore:
  - "tests/**"
